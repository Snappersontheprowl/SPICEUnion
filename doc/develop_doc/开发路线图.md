# SPICEUnion 开发路线图

更新时间：2026-08-06

本文是 SPICEUnion 的施工路线图。它只记录当前有效阶段、产出、验证命令和完成定义。

## 1. 文档分工

| 文档 | 职责 |
|---|---|
| `README.md` | 项目入口与构建命令 |
| `TODO` | 当前未完成事项、暂缓池、监控项 |
| `doc/develop_doc/当前事实状态.md` | 当前已实现事实、验证结果、fixture 来源与边界 |
| `doc/develop_doc/CPP版本开发计划书.md` | 项目范围、架构边界、里程碑 |
| `doc/develop_doc/开发路线图.md` | 后续施工步骤、产出与完成定义 |
| `doc/develop_doc/OrderedConcurrentPool开发路线图.md` | OrderedConcurrentPool 独立化专项路线 |

## 2. 阶段状态

| 阶段 | 状态 | 主要产出 |
|---|---|---|
| M0 | 已完成 | CMake / GoogleTest 骨架 |
| M1.0 | 已完成 | evaluator core types、fake-session 契约测试 |
| M1.1 | 已完成 | Spectre interactive lifecycle |
| M1.2 | 已完成 | Spectre 参数写入、单任务运行、失败状态 |
| M1.3 | 已完成 | Spectre multi-worker evaluator |
| M2.1 | 已完成 | ResultIR、`ReadResult<T>`、reader API 骨架 |
| M2.2 | 已完成 | `.raw` 定位、AC helper、UGBW/PM、settling time |
| M2.3 | 已完成 | 可选 libpsf backend、PSF fixture tests |
| M3.0 | 已完成 | Ngspice RC AC batch backend |
| M3.1 | 已完成 | Ngspice RC TRAN batch backend |
| M3.2 | 已完成 | Spectre / Ngspice AC 语义对照 |
| M3.3 | 已完成 | Spectre / Ngspice TRAN 语义对照 |
| M3.4 | 已完成 | Ngspice DC sweep 与 `DcSweep` |
| M3 收敛 | 待评估 | Spectre DC sweep 对照、MOS I-V 需求判断 |
| M3.5.0 | 已完成 | `SimulatorPool` 行为契约测试 |
| M3.5.1 | 已完成 | SPICEUnion 内部领域无关 `OrderedConcurrentPool` 核心 |
| M3.5.2 | 已完成 | `SimulatorPool` 改为 `OrderedConcurrentPool` adapter |
| M3.5.3 | 已完成 | default / external / libpsf 验证与边界检查 |
| M3.5.4 | 已完成 | 创建独立 `OrderedConcurrentPool` 项目并验证 |
| M3.5.5 | 已完成 | SPICEUnion 装配外部 `OrderedConcurrentPool` 项目并移除内部副本 |
| M3.5.6 | 暂缓 | `OrderedConcurrentPool` 发布准备、许可证与 benchmark |
| M4 | 暂缓 | C ABI / Python binding |
| M5 | 暂缓 | benchmark 与发布形态 |

## 3. 当前后续任务

### 3.1 Spectre 侧 DC sweep 对照评估

目标：

- 判断是否需要增加 Spectre 电阻分压 DC sweep fixture；
- 验证 Spectre PSF DC sweep 是否能映射到现有 `DcSweep`；
- 不引入 MOS 模型依赖。

预期产出：

- Spectre 源 netlist fixture；
- 固化 PSF fixture；
- `DcSweep` 语义对照测试；
- `doc/develop_doc/当前事实状态.md` 更新。

完成定义：

- 默认构建不依赖 Spectre；
- libpsf 构建下能读取 Spectre DC sweep fixture；
- external 构建下 Ngspice DC sweep 仍通过；
- Spectre / Ngspice DC sweep 复用同一套语义检查。

暂缓条件：

- 当前没有跨 backend DC sweep 消费者；
- libpsf 无法读取目标 Spectre DC sweep 格式；
- fixture 会引入 PDK 或模型依赖。

### 3.2 MOS I-V / gm/Id 需求评估

目标：

- 判断 `DcSweep` 是否足够表达 MOS I-V；
- 若出现 family curve 需求，再设计多曲线 DC ResultIR。

不得提前做：

- 不提前引入 MOS 模型库；
- 不提前引入多维 sweep；
- 不把 gm/Id lookup table 写进 SPICEUnion core。

完成定义：

- 有真实消费者；
- 有至少一个真实 backend 输出样本；
- 新 ResultIR 有测试覆盖；
- README 与当前事实状态同步更新。

### 3.3 EvaluatorOptions / netlist template 扩展

进入条件：

- Spectre 与 Ngspice 两个 backend 都需要同类配置能力；
- 现有 `ParameterState` 与内置任务枚举无法表达真实需求。

不得提前做：

- 不为了未来仿真器设计完整 request IR；
- 不把 netlist 生成器变成项目业务 DSL；
- 不破坏现有 `Evaluator` 输入保序与 failure isolation 契约。

## 4. 暂缓任务

| 任务 | 暂缓原因 |
|---|---|
| legacy sensitivity 读取 | 缺可信参考值与 fixture |
| Spectre 23.1 PSFXL transient 解析 | 当前已有 unsupported fixture，尚无转换路径 |
| 原生 PSF parser | libpsf backend 当前足够覆盖已验证普通 PSF fixture |
| C ABI / pybind11 | C++ API 仍在收敛 |
| OrderedConcurrentPool 发布准备 | 许可证、benchmark 与发布口径尚未完成 |
| Xyce / Hspice backend | 当前无真实使用需求 |

## 5. 开发规则

- 大改前先在根 `TODO` 写分步骤计划。
- 行为、结构、入口、配置或流程变化后同步 README / 当前事实状态 / 相关目录 README。
- 每个实现阶段必须有测试或可检查的文档产出。
- 默认测试不得依赖外部 EDA 工具。
- external 测试必须在依赖缺失时 skip。
- 未实测性能数字不得写成完成事实。
- 完成修改后按项目约定提交本地 git commit。

## 6. 验证矩阵

| 场景 | 命令 |
|---|---|
| 默认开发验证 | `cmake --preset default && cmake --build --preset default && ctest --preset default --output-on-failure` |
| 外部 Spectre / Ngspice 验证 | `cmake --preset external && cmake --build --preset external && ctest --preset external --output-on-failure` |
| libpsf backend 验证 | `cmake -S . -B cmake-build-libpsf -DCMAKE_BUILD_TYPE=Debug -DCMAKE_EXPORT_COMPILE_COMMANDS=ON -DSPICEUNION_BUILD_TESTS=ON -DSPICEUNION_ENABLE_EXTERNAL_TESTS=OFF -DSPICEUNION_ENABLE_LIBPSF_READER=ON && cmake --build cmake-build-libpsf && ctest --test-dir cmake-build-libpsf --output-on-failure` |

最近记录结果：

| 配置 | 结果 |
|---|---|
| default | `100% tests passed, 0 tests failed out of 80` |
| external | `100% tests passed, 0 tests failed out of 80` |
| libpsf | `100% tests passed, 0 tests failed out of 90` |

## 7. 文档更新规则

新增或变更能力时：

1. 更新根 `README.md` 的当前能力或边界。
2. 更新 `doc/develop_doc/当前事实状态.md`。
3. 若影响计划或阶段状态，更新本路线图。
4. 若影响公开 API，更新 `include/su/README.md`。
5. 若影响测试结构，更新 `tests/README.md`。
