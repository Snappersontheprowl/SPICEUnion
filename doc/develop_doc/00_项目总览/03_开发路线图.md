# SPICEUnion 开发路线图

更新时间：2026-09-02

本文只记录下一步施工路线、进入条件、完成定义、暂缓条件和验证矩阵。

当前已完成事实见 `01_当前事实状态.md`；当前架构见 `02_架构总览.md`；项目长期边界见
`00_项目章程.md`。

## 1. 当前阶段状态

| 阶段 | 状态 | 责任文档 |
|---|---|---|
| M1 Spectre 执行层 | 已完成 | `../10_阶段记录/01_M1_Spectre执行层.md` |
| M2 ResultIR 与结果读取 | 已完成 | `../10_阶段记录/02_M2_ResultIR与结果读取.md` |
| M3 多仿真器与语义收敛 | 已完成基础闭环 | `../10_阶段记录/03_M3_多仿真器与语义收敛.md` |
| M4 语言绑定 | M4.2 已完成；执行层 binding 暂缓 | `../10_阶段记录/04_M4_语言绑定.md` |
| M5 用户工作流 API | C++ facade 已完成；Python / MATLAB 暂缓 | `../20_专题记录/02_用户工作流API设计.md` |
| OrderedConcurrentPool 专项 | 已完成独立化与外部装配 | `../20_专题记录/01_OrderedConcurrentPool专项.md` |

根 `TODO` 是当前活跃任务和暂缓池的短清单；本文件只维护路线级说明。

## 2. 候选下一阶段

### 2.1 Python workflow binding

目标：让 Python 用户能够直接启动批量仿真，而不是只读取已有结果。Python 入口应绑定
workflow 层，而不是直接暴露底层 `SimulatorSession`、`SessionFactory` 或
`OrderedConcurrentPool`。

推荐前置条件：

- C++ workflow API 经过真实 Spectre / Ngspice 使用复核。
- Python result 对象是否直接复用当前读取层包装已收口。
- Python 生命周期语义与 C++ `Simulation.cleanup()` 的关系已收口。

完成定义：

- Python 可创建 simulation、声明参数、提交 `list[dict[str, float]]` 并获取结果对象。
- Python 结果对象复用当前 `ok()`、`status_text()`、`message` 失败语义。
- CTest 覆盖 import、API contract 和最小执行层 smoke。

### 2.2 结果格式能力扩展

候选方向：

- Spectre 23.1 PSFXL transient 解析或转换路径。
- 原生 PSF parser。
- legacy sensitivity 读取。
- libpsf backend 产品化分发与 license notice。

进入条件：

- 有真实 fixture 或真实消费者。
- 能明确和现有 `ResultIR` 的关系。
- 能用默认测试或可选预设钉住失败语义。

### 2.3 仿真器与结果模型扩展

候选方向：

- Ngspice `.op` operating point。
- MOS I-V / gm/Id 多曲线 DC 结果。
- Xyce / Hspice backend。

进入条件：

- 有合法网表样本和固定 fixture。
- 现有 `ScalarResult`、`DcSweep`、`AcResponse`、`TranWaveform` 无法表达真实需求。
- 新 backend 或 ResultIR 不把业务 metric / objective / optimizer 写进 core。

## 3. 暂缓池维护

暂缓事项只在根 `TODO` 保留短清单。本路线图只记录同类方向的进入条件和完成定义。

暂缓项被激活时：

1. 在根 `TODO` 写分步骤实施计划。
2. 必要时在 `20_专题记录/` 新增草案或专题文档。
3. 实现完成后更新 `01_当前事实状态.md` 和相关 README。
4. 若改变架构，更新 `02_架构总览.md`。
5. 清理根 `TODO` 活跃项。

## 4. 验证矩阵

| 场景 | 命令 |
|---|---|
| 默认开发验证 | `cmake --preset default && cmake --build --preset default && ctest --preset default --output-on-failure` |
| 外部 Spectre / Ngspice 验证 | `cmake --preset external && cmake --build --preset external && ctest --preset external --output-on-failure` |
| libpsf backend 验证 | `cmake --preset libpsf && cmake --build --preset libpsf && ctest --preset libpsf --output-on-failure` |
| Python binding 验证 | `cmake --preset python && cmake --build --preset python && ctest --preset python --output-on-failure` |
| Python binding + libpsf 验证 | `cmake --preset python-libpsf-pic && cmake --build --preset python-libpsf-pic && ctest --preset python-libpsf-pic --output-on-failure` |

最近验证结果不在本文件维护，统一见 `01_当前事实状态.md`。

## 5. 文档收口规则

新增或变更能力时：

1. 更新根 `README.md` 的当前能力、边界或主示例。
2. 更新 `01_当前事实状态.md`。
3. 若影响总体分层、执行链路、结果读取链路或模块责任，更新 `02_架构总览.md`。
4. 若影响施工顺序、进入条件或完成定义，更新本路线图。
5. 若影响项目范围或设计原则，更新 `00_项目章程.md`。
6. 若影响公开 API，更新 `include/su/README.md`。
7. 若影响测试结构，更新 `tests/README.md`。
8. 若影响阶段或专题边界，更新 `../10_阶段记录/` 或 `../20_专题记录/` 下对应文档。

完成修改后按项目约定进行本地 `git commit`。
