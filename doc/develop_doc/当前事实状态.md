# SPICEUnion 当前事实状态

更新时间：2026-08-12

## 项目定位

SPICEUnion 是 C++17 仿真器执行与结果读取基础设施库。

| 范围内 | 范围外 |
|---|---|
| batch 参数状态执行 | circuit metric / objective / penalty |
| worker 工作目录隔离 | optimizer |
| 输入顺序保序返回 | PDK 内容管理 |
| Spectre interactive backend | GUI |
| Ngspice batch backend | 完整 netlist IR |
| 最小 ResultIR 与结果读取 helper | Hspice / Xyce backend |

## 当前规划状态

- M3.0-M3.4 基础闭环已完成，M3 后续只做 Spectre 侧 DC sweep 最小收口，责任文档为 `M3.md`。
- M4.0 pybind11 与 C ABI 接入决策已收口，责任文档为 `M4.md`。
- M4.1 待启动方向为 pybind11 最小结果读取绑定。
- C ABI 第一版暂缓，只保留 ownership 草案。

## 当前已实现能力

### 执行层

- `Evaluator` 支持 batch execution facade。
- batch 结果按输入顺序返回。
- worker 使用独立 work directory。
- 单任务失败不会破坏其他任务结果。
- startup failure、timeout、transport failure、simulation failure 有标准 `TaskStatus`。
- SPICEUnion 通过 `SPICEUNION_ORDERED_POOL_SOURCE_DIR` 装配外部
  `~/my_lab/projects/OrderedConcurrentPool` 项目。
- `spiceunion_core` 链接 `ocp::ordered_concurrent_pool`。
- `SimulatorPool` 已改为 SPICEUnion adapter，负责把 `EvaluatorOptions`、
  `SessionFactory`、`ParameterState`、`TaskResult` 与 `SimulatorSession` 映射到
  `OrderedConcurrentPool`。
- `~/my_lab/projects/OrderedConcurrentPool` 已创建为独立项目：
  - C++17 header-only；
  - CMake target 为 `ocp::ordered_concurrent_pool`；
  - 包含 `include/ocp/ordered_concurrent_pool.hpp`；
  - 包含 GoogleTest 契约测试；
  - 包含 `examples/basic_batch.cpp` 最小示例；
  - 包含 `benchmarks/ordered_pool_benchmark.cpp` 最小基准程序；
  - license 已明确为 MIT；
  - 已记录 `CHANGELOG.md`；
  - 已将 `main` 与 `v0.1.0` tag 发布到 `origin`；
  - 已增加 GitHub Actions CI；
  - 可独立 configure / build / test / install。
- `SpectreSession` 支持 `spectre +interactive`：
  - SKILL handshake；
  - circuit handle 初始化；
  - 参数写入；
  - `(sclRun "all")`；
  - completion 判断；
  - graceful / force stop。
- `NgspiceSession` 支持 `ngspice -b` batch mode。

### ResultIR：`include/su/result.hpp`

| 类型 | 含义 |
|---|---|
| `ScalarResult` | 单 signal 标量 |
| `DcSweep` | 单 sweep axis、单 signal 实数响应 |
| `AcResponse` | frequency + complex response |
| `AcDerivedView` | magnitude dB + phase deg 派生视图 |
| `TranWaveform` | time/value 波形 |
| `SensitivityEntry` | legacy sensitivity 原始条目结构 |
| `ReadResult<T>` | 带显式 `ResultStatus` 的读取结果 |

### Result helper

已实现：

- `.raw` 目录定位；
- `dcOp.dc` 单 signal 标量读取；
- swept complex PSF 读取；
- 普通 time-sweep PSF transient 读取；
- AC magnitude / phase 派生；
- UGBW / phase margin；
- settling time；
- Ngspice `wrdata` AC / TRAN / DC sweep 文本读取。

libpsf 事实：

| 项 | 状态 |
|---|---|
| backend | 可选内部 backend |
| 默认构建 | 不编译、不链接 |
| 启用开关 | `SPICEUNION_ENABLE_LIBPSF_READER=ON` |
| 公开 API | 不暴露 libpsf 类型 |
| 本机默认路径 | `local/external/libpsf/install` |

### Spectre backend

当前已验证：

- interactive handshake / stop；
- single-task `(sclRun "all")`；
- evaluator multi-worker batch；
- PSF `dcOp.dc` scalar fixture；
- PSF `ac.ac` / `stb.stb` swept complex fixture；
- 普通 PSF transient fixture；
- Spectre RC low-pass AC fixture 可读入 `AcResponse`；
- Spectre RC charging TRAN fixture 可读入 `TranWaveform`。

当前边界：

- Spectre 23.1 PSFXL transient fixture 当前明确返回 `kUnsupportedFormat`。
- legacy sensitivity 仍未实现。
- Spectre 侧 DC sweep 与 Ngspice `DcSweep` 尚未做同类对照。

### Ngspice backend

当前 `NgspiceBuiltinTask` 输出映射：

| 任务 | 输出文件 | 输出格式 | ResultIR |
|---|---|---|---|
| `kRcAc` | `rc_ac.out` | 三列 `frequency real imag` | `AcResponse` |
| `kRcTran` | `rc_tran.out` | 两列 `time value` | `TranWaveform` |
| `kResistorDividerDc` | `resistor_divider_dc.out` | 两列 `sweep_value value` | `DcSweep` |

当前 external 测试会真实调用 Ngspice 并验证：

- RC AC 的 -3 dB 频率；
- RC TRAN 的 `τ` / `5τ` 充电曲线；
- 电阻分压 DC sweep 的分压比例。

## 已提交 fixture

PSF fixture 位于 `tests/fixtures/psf/`：

| fixture | 作用 |
|---|---|
| `dc_op_minimal.raw/dcOp.dc` | 最小 DC scalar 读取 |
| `spectre_materials_dc_op.raw/dcOp.dc` | spectre_materials 历史 DC scalar |
| `amp_ac_response.raw/ac.ac` | spectre_materials 历史 AC response |
| `spectre_rc_lowpass_ac.raw/ac.ac` | Spectre RC AC 语义测试 |
| `spectre_materials_stb_loop_gain.raw/stb.stb` | STB swept complex response |
| `tran_time_sweep.raw/tran.tran` | 普通 transient waveform |
| `spectre_rc_charging_tran.raw/tran.tran.tran` | Spectre RC TRAN 语义测试 |
| `spectre_materials_psfxl_tran.raw/tran.tran.tran` | PSFXL unsupported 边界 |

Spectre 源 fixture 位于 `tests/fixtures/spectre/`：

- `rc_lowpass_ac/input.scs`
- `rc_charging_tran/input.scs`

## 构建与验证

默认测试：

```bash
cmake --preset default
cmake --build --preset default
ctest --preset default --output-on-failure
```

```text
100% tests passed, 0 tests failed out of 80
```

外部测试：

```bash
cmake --preset external
cmake --build --preset external
ctest --preset external --output-on-failure
```

```text
100% tests passed, 0 tests failed out of 80
```

libpsf full：

```bash
cmake -S . -B cmake-build-libpsf \
  -DCMAKE_BUILD_TYPE=Debug \
  -DCMAKE_EXPORT_COMPILE_COMMANDS=ON \
  -DSPICEUNION_BUILD_TESTS=ON \
  -DSPICEUNION_ENABLE_EXTERNAL_TESTS=OFF \
  -DSPICEUNION_ENABLE_LIBPSF_READER=ON
cmake --build cmake-build-libpsf
ctest --test-dir cmake-build-libpsf --output-on-failure
```

```text
100% tests passed, 0 tests failed out of 90
```

OrderedConcurrentPool 独立项目：

```bash
cd ~/my_lab/projects/OrderedConcurrentPool
cmake -S . -B build-release-check -DCMAKE_BUILD_TYPE=Release -DCMAKE_EXPORT_COMPILE_COMMANDS=ON
cmake --build build-release-check
ctest --test-dir build-release-check --output-on-failure
```

```text
100% tests passed, 0 tests failed out of 8
```

OrderedConcurrentPool benchmark 验证：

```bash
cd ~/my_lab/projects/OrderedConcurrentPool
cmake -S . -B build-benchmark \
  -DCMAKE_BUILD_TYPE=Release \
  -DORDERED_CONCURRENT_POOL_BUILD_TESTS=OFF \
  -DORDERED_CONCURRENT_POOL_BUILD_EXAMPLES=OFF \
  -DORDERED_CONCURRENT_POOL_BUILD_BENCHMARKS=ON
cmake --build build-benchmark
./build-benchmark/ordered_concurrent_pool_benchmark
```

说明：benchmark 输出为 CSV，只用于本机回归观察，不作为跨机器性能承诺。

OrderedConcurrentPool install 验证：

```bash
cd ~/my_lab/projects/OrderedConcurrentPool
cmake --install build-release-check --prefix /tmp/ocp_release_install_...
```

安装产物包含：

- `include/ocp/ordered_concurrent_pool.hpp`
- `lib64/cmake/OrderedConcurrentPool/OrderedConcurrentPoolConfig.cmake`
- `lib64/cmake/OrderedConcurrentPool/OrderedConcurrentPoolConfigVersion.cmake`
- `lib64/cmake/OrderedConcurrentPool/OrderedConcurrentPoolTargets.cmake`
- `share/doc/OrderedConcurrentPool/LICENSE`
- `share/doc/OrderedConcurrentPool/README.md`
- `share/doc/OrderedConcurrentPool/CHANGELOG.md`

OrderedConcurrentPool GitHub Actions：

| Run | Commit | 结果 |
|---|---|---|
| `31111814593` | `0524272` | Release build/test、install consumer、ASan、TSan 均通过 |

## 当前未实现事项

- legacy sensitivity 读取。
- Spectre 23.1 PSFXL transient 解析或转换。
- 原生 PSF parser。
- C ABI 正式实现。
- Python / pybind11 binding（M4.1 待启动）。
- Ngspice `.op` operating point。
- MOS I-V / gm/Id 多曲线 DC 结果。
- Spectre 与 Ngspice 同类 DC sweep 语义对照。
- Xyce / Hspice backend。
