# src/core

Core execution-layer implementation lives here.

This directory should remain independent of any specific simulator process when
possible. Simulator-specific behavior belongs in `src/session/`.
