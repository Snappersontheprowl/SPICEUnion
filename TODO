# TODO

## 当前阶段

SPICEUnion 已完成 M0、M1.0、M1.1。

当前目标进入 M1.2：在单个真实 Spectre session 上完成参数写入、
`(sclRun "all")`、完成等待与失败恢复。

## 当前活跃待办

### M1.2 Spectre 单任务运行

- [ ] 为 `ParameterState` 生成 SKILL 参数写入命令：
      `(sclSetAttribute (sclGetParameter top "key") "value" <value>)`。
- [ ] 实现 `(sclRun "all")` 运行触发。
- [ ] 等待完成标志：资源统计行后 `t`。
- [ ] 识别 `nil` 为单任务失败。
- [ ] 识别 EOF / BrokenPipe / timeout，并回收进程。
- [ ] 实现有限次 restart attempt。
- [ ] 补充协议单测与真实 Spectre 单任务运行测试。

### M1.3 并发 Evaluator 真实路径

- [ ] 将真实 `SpectreSession` 接入 evaluator / pool 工厂。
- [ ] 用真实 session 回归多 worker batch。
- [ ] 初步设计 C ABI handle 与 `TaskResult` 输出结构。
- [ ] 文档化 C ABI 所有权规则。

## 暂缓池

- [ ] 真实 `SpectreSession` 的参数写入、运行、完成等待、transport failure 恢复。
- [ ] 原生 PSF parser。
- [ ] C ABI 稳定化与 pybind11 绑定。
- [ ] Ngspice / Xyce / Hspice 多仿真器扩展。
- [ ] `OrderedConcurrentPool` 独立仓库提取。

## 监控项

- [ ] `spectre_materials` 行为基线变化时，同步审视 SPICEUnion 计划书与契约测试。
- [ ] 性能数字未实测前，只能作为目标值，不得写成已完成结果。
