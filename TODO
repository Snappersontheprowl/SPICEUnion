# TODO

## 当前阶段

SPICEUnion 已完成 M0、M1.0、M1.1、M1.2、M1.3、M2.1、M2.2、M2.3，并已完成
M3.0 的 Ngspice 最小 batch adapter。

M3.0 已用公开可运行的 RC low-pass AC 示例验证现有 `SimulatorSession` / evaluator /
ResultIR 抽象可以承载第二个真实仿真器。

Python `task_library.py` 只作为历史参考和 fixture 来源；C++ API 不为强行兼容
Python 返回习惯而牺牲类型安全。

## 当前活跃待办

### M3 后续收敛

- [ ] 比较 Spectre / Ngspice 在同类 AC 或 TRAN 任务上的 IR 复用情况。
- [ ] 评估是否增加 Ngspice `.tran` RC charging 示例并映射到 `TranWaveform`。
- [ ] 评估是否增加 Ngspice `.dc` 简单 sweep 示例；只有存在真实消费者时才扩展
      ResultIR。
- [ ] 只有两个真实 backend 共同需要时，再扩展 `EvaluatorOptions` 或引入 netlist
      模板接口。

## 暂缓池

- [ ] legacy sensitivity fixture、可信参考值与读取实现。
- [ ] Spectre 23.1 PSFXL transient 的解析或转换路径评估。
- [ ] C++ helper 与 Python helper 在失败语义上的有意差异集中记录。
- [ ] 原生 PSF parser。
- [ ] C ABI 稳定化与 pybind11 绑定。
- [ ] evaluator C ABI 所有权草案。
- [ ] libpsf backend 产品化分发与 license notice。
- [ ] Xyce / Hspice 多仿真器扩展。
- [ ] `OrderedConcurrentPool` 独立仓库提取。

## 监控项

- [ ] `spectre_materials` 行为基线变化时，同步审视 SPICEUnion 计划书与契约测试。
- [ ] 性能数字未实测前，只能作为目标值，不得写成已完成结果。
