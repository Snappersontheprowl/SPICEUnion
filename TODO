# TODO

## 当前阶段

SPICEUnion 已完成 M0、M1.0、M1.1、M1.2、M1.3。

当前目标进入 C ABI 所有权草案与 M2：最小结果 IR、PSF helper、
与 Python `task_library.py` 行为的一致性测试。

## 当前活跃待办

### C ABI 所有权草案

- [ ] 明确 evaluator handle 创建 / 销毁 API。
- [ ] 明确 `TaskResult` 跨 C 边界的数据结构。
- [ ] 明确 string / array 内存由谁分配、谁释放。
- [ ] 编写 `include/su/evaluator.h` 草案。
- [ ] 编写 C ABI 文档或 README 小节。

### M2 最小结果 IR 与 PSF helper

- [ ] 定义最小结果 IR：scalar、AC response、tran waveform、sensitivity entry。
- [ ] 实现 `.raw` 结果目录定位。
- [ ] 实现 `dcOp.dc` 单信号读取。
- [ ] 实现 `ac.ac` 频率、幅度、相位读取。
- [ ] 实现 UGBW / phase margin 计算。
- [ ] 实现 `tran.tran` 建立时间计算。
- [ ] 实现 legacy sensitivity 结果读取。
- [ ] 建立与 Python `task_library.py` 的一致性测试 fixture。

## 暂缓池

- [ ] 原生 PSF parser。
- [ ] C ABI 稳定化与 pybind11 绑定。
- [ ] Ngspice / Xyce / Hspice 多仿真器扩展。
- [ ] `OrderedConcurrentPool` 独立仓库提取。

## 监控项

- [ ] `spectre_materials` 行为基线变化时，同步审视 SPICEUnion 计划书与契约测试。
- [ ] 性能数字未实测前，只能作为目标值，不得写成已完成结果。
