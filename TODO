# TODO

## 当前阶段

SPICEUnion 已完成 M0、M1.0、M1.1、M1.2、M1.3、M2.1。

当前目标进入 M2.2：纯路径与纯数学 helper。

Python `task_library.py` 只作为历史参考和 fixture 来源；C++ API 不为强行兼容
Python 返回习惯而牺牲类型安全。

## 当前活跃待办

### M2.2 纯路径与纯数学 helper

- [ ] 实现 `.raw` 结果目录定位。
- [ ] 实现 UGBW / phase margin 计算。
- [ ] 实现 waveform settling time 计算。
- [ ] 覆盖空输入、长度不一致、无 crossing、边界目标值等失败/边界测试。

### M2.3 最小文件读取能力

- [ ] 按 `doc/develop_doc/M2-libpsf评估与接入策略.md` 完成 libpsf spike。
- [ ] 确认 libpsf C++ API 是否能读取当前 dc/ac/tran/sens fixture。
- [ ] 设计 `SPICEUNION_ENABLE_LIBPSF_READER` 可选开关，不影响默认构建。
- [ ] 实现 `dcOp.dc` 单信号读取。
- [ ] 实现 `ac.ac` frequency 与 complex response 读取。
- [ ] 实现 `tran.tran` time axis 与 waveform 读取。
- [ ] 实现 legacy sensitivity 结果读取。
- [ ] 建立固定 fixture 与可信参考数值。
- [ ] 记录 C++ helper 与 Python helper 在失败语义上的有意差异。

## 暂缓池

- [ ] 原生 PSF parser。
- [ ] C ABI 稳定化与 pybind11 绑定。
- [ ] evaluator C ABI 所有权草案。
- [ ] libpsf backend 产品化分发与 license notice。
- [ ] Ngspice / Xyce / Hspice 多仿真器扩展。
- [ ] `OrderedConcurrentPool` 独立仓库提取。

## 监控项

- [ ] `spectre_materials` 行为基线变化时，同步审视 SPICEUnion 计划书与契约测试。
- [ ] 性能数字未实测前，只能作为目标值，不得写成已完成结果。
